extends layout
block above-content

block header 
  header.hero.login
    h2 Log in

block content
  .section.intro.slanted.margin-vert
    .content.text-center
        form.form.form-signin(method='post')
          h2 Please sign in
          .row
            input#input_username.form-control(name='email', placeholder='email', type='text', required='', autofocus='')
          .row 
            input#input_password.form-control(name='password', placeholder='Password', type='password', required='')
          button.button-primary.login(type='submit') Sign in
        p.login-separator or
        a.button.button-primary(href='/register') Activate your ticket
